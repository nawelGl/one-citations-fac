services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"

  profile:
    build: ./profiles-api
    ports:
      - "9080:9090"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    depends_on:
      - mongo